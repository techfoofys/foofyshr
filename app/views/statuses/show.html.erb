<div class="container">
  <div class=" jumbotron">
    <p>
      <strong>Title:</strong>
      <%= @status.title %>
    </p>

    <p >
      <strong >Color:</strong>
      <div class="" style="background-color: <%= @status.color %>">
        <%= @status.color %>
      </div>

    </p>

    <%= link_to 'Edit', edit_status_path(@status) %> |
    <%= link_to 'Back', statuses_path %>
  </div>
</div>
