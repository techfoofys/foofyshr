<p id="notice"><%= notice %></p>

<h1>Instalments</h1>

<table>
  <thead>
    <tr>
      <th>Project</th>
      <th>Instalment date</th>
      <th>Amount</th>
      <th>Instalment no</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @instalments.each do |instalment| %>
      <tr>
        <td><%= instalment.project %></td>
        <td><%= instalment.instalment_date %></td>
        <td><%= instalment.amount %></td>
        <td><%= instalment.instalment_no %></td>
        <td><%= link_to 'Show', instalment %></td>
        <td><%= link_to 'Edit', edit_instalment_path(instalment) %></td>
        <td><%= link_to 'Destroy', instalment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Instalment', new_instalment_path %>
